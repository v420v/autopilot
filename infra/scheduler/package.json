{
  "name": "autopilot-scheduler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that dispatches autopilot's GitHub Actions workflows on a schedule.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5"
  }
}
